<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <form action="" method="post">
      <input
        type="text"
        name="type"
        placeholder="Type"
        value="<%= locals.transaction[1] %>"
        required
      />
      <input
        type="number"
        name="amount"
        placeholder="Amount"
        value="<%= locals.transaction[2] %>"
        required
      />
      <input
        type="date"
        name="date"
        value="<%= new Date(locals.transaction[3]).toISOString().split('T')[0] %>"
        required
      />
      <button type="submit">Edit</button>
    </form>
  </body>
</html>
